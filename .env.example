# Server configuration
NODE_ENV=development
# Port used by the server container and published on the host when running via Docker Compose
PORT=4000
JWT_SECRET=super-secret
JWT_EXPIRES_IN=1d
# Comma-separated list of allowed origins for CORS
CORS_ORIGINS=http://localhost:5173,http://localhost:4173
LOG_LEVEL=info
DATABASE_URL=postgres://sayabasu:example@0.0.0.0.0:5432/soulstone_db

# Client configuration
VITE_API_BASE_URL=http://localhost:4000/api
# Port used by the client container and published on the host when running via Docker Compose
CLIENT_PORT=5173
