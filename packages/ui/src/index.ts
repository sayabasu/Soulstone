/**
 * Placeholder export for the shared UI package. Component implementations will live here
 * as the design system evolves. Keeping the module minimal allows downstream applications
 * to import from `@soulstone/ui` without bundler errors while the foundation is established.
 */
export const placeholder = true;
